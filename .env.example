# .env.example — 复制成 .env 填你的 Gotify 配置（docker compose 自动读同目录 .env）。
# .env 别入库（已 gitignore，含 token）。两个都留空也行（等 App 上报）。

# Gotify CLIENT token（必填；Gotify WebUI → CLIENTS → 建 Client → 复制，不是 app token）
GOTIFY_CLIENT_TOKEN=

# Gotify 地址（桥连 Gotify 用）：
#   远程/域名：      https://gotify.你域名.com
#   同机另一容器：    http://gotify容器名:端口   （需把它们放同一 docker network）
#   宿主机上的 Gotify：http://host.docker.internal:端口   （compose 已开 host-gateway）
# ⚠️ 别填 127.0.0.1/localhost——容器里那是容器自己，连不到 Gotify。
GOTIFY_HTTP_URL=
