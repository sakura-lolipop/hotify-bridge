# === 机密：永不入库 ===
private.json
*_private.json
*private.json
private.md
bridge_config.json
bridge_config.yaml
push_tokens.json
agconnect-services.json

# === Python ===
__pycache__/
*.pyc
.venv/
venv/

# === PyInstaller 构建产物（本地打包用；CI 在 runner 里打、产物不落本地）===
build/
dist/
*.spec

# === 本地自测 Gotify 实例沙箱（整个 test_local/：config/selftest/USAGE/push_lockscreen 含 token·内网IP·设备uuid，全本地不入公开仓）===
test_local/

# === 临时 / 日志 ===
*.log
.env

# === cloud_function_urls fetch 缓存（本地生成,不入库）===
cloud_function_urls.cache.txt

# === Go 构建产物（CP0 起，Go 桥在 go/ 子目录；源码 go.mod/*.go 入库）===
go/gotify-bridge.exe
go/gotify-bridge
go/*.test
go/*.out
go/dist/
